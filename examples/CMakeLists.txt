# Set the install directory for the examples.
set(EXAMPLES_INSTALL_DIR "${CMAKE_SOURCE_DIR}/install/examples/")

# Add each example to configure.
add_subdirectory(01-hello-world)
add_subdirectory(02-scenes-and-systems)
